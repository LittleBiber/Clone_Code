import React from 'react'

import { SelectOauth } from './index'

export default {
  title: 'SignUp/SelectOauth',
  component: SelectOauth,
}

const Template = (args) => <SelectOauth {...args} />

export const Default = Template.bind({})
Default.args = {}
