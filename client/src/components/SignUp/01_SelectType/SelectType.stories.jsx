import React from 'react'

import { SelectType } from './index'

export default {
  title: 'SignUp/SelectType',
  component: SelectType,
}

const Template = (args) => <SelectType {...args} />

export const Default = Template.bind({})
Default.args = {}
