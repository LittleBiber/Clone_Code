import React from 'react'

import { NarrowMenuList } from './index'

export default {
  title: 'Footer/NarrowMenuList',
  component: NarrowMenuList,
}

const Template = (args) => <NarrowMenuList {...args} />

export const Default = Template.bind({})
Default.args = {}
