import React from 'react'

import { Questions } from './index'

export default {
  title: 'Pricing/03_Questions',
  component: Questions,
}

const Template = (args) => <Questions {...args} />

export const Default = Template.bind({})
Default.args = {}
