import React from 'react'

import { NarrowSelector } from './index'

export default {
  title: 'Pricing/01_Plans/NarrowSelector',
  component: NarrowSelector,
}

const Template = (args) => <NarrowSelector {...args} />

export const Default = Template.bind({})
Default.args = {}
