import React from 'react'

import { Tables } from './index'

export default {
  title: 'Pricing/02_Features/Tables',
  component: Tables,
}

const Template = (args) => <Tables {...args} />

export const Default = Template.bind({})
Default.args = {}
