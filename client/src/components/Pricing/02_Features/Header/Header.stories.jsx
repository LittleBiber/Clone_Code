import React from 'react'

import { Header } from './index'

export default {
  title: 'Pricing/02_Features/Header',
  component: Header,
}

const Template = (args) => <Header {...args} />

export const Default = Template.bind({})
Default.args = {}
