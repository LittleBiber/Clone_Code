import React from 'react'

import { Guarantee } from './index'

export default {
  title: 'Legality/03_Guarantee',
  component: Guarantee,
}

const Template = (args) => <Guarantee {...args} />

export const Default = Template.bind({})
Default.args = {}
