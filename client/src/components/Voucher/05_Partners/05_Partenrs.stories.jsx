import React from 'react'

import { Partners } from './index'

export default {
  title: 'Voucher/05_Partners',
  component: Partners,
}

const Template = (args) => <Partners {...args} />

export const Default = Template.bind({})
Default.args = {}
