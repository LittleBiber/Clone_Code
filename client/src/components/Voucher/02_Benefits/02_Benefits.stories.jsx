import React from 'react'

import { Benefits } from './index'

export default {
  title: 'Voucher/02_Benefits',
  component: Benefits,
}

const Template = (args) => <Benefits {...args} />

export const Default = Template.bind({})
Default.args = {}
