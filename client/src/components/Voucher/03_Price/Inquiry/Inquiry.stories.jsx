import React from 'react'

import { Inquery } from './index'

export default {
  title: 'Voucher/03_Price/Inquery',
  component: Inquery,
}

const Template = (args) => <Inquery {...args} />

export const Default = Template.bind({})
Default.args = {}
